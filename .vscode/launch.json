{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "java",
      "name": "Run API Gateway (local)",
      "request": "launch",
      "mainClass": "cl.smid.apigateway.ApiGatewayApplication",
      "projectName": "api-gateway",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    }
  ]
}
